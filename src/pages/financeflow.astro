---
import Layout from "../layouts/Layout.astro";

const modules = [
  {
    title: "Facturación y Presupuestos",
    image: "/financeflow.svg",
    desc: "Gestión completa del ciclo de ventas, desde presupuesto hasta cobro.",
    points: [
      "Facturas con estados (Borrador, Enviada, Pagada, Vencida)",
      "Presupuestos y conversión a factura en 1 clic",
      "PDF, envío por email y duplicado",
      "Facturas recurrentes y vista pública segura",
      "Pagos online con Stripe",
    ],
  },
  {
    title: "Gestión Financiera",
    image: "/financeflow.svg",
    desc: "Control de gastos, impuestos y previsiones fiscales en tiempo real.",
    points: [
      "Registro de gastos con categorías y adjuntos",
      "Cálculo de IVA repercutido / soportado",
      "Estimación de IRPF y resultado fiscal",
      "Hucha fiscal para previsión trimestral",
      "Exportación de reportes (Excel/CSV)",
    ],
  },
  {
    title: "CRM y RRHH",
    image: "/financeflow.svg",
    desc: "Clientes, empleados, roles y control horario desde una sola plataforma.",
    points: [
      "Fichas de cliente e historial de facturación / deuda",
      "Gestión de empleados y roles",
      "Control horario START/STOP",
      "Geolocalización en fichajes (GPS)",
      "Panel de fichajes del empleado",
    ],
  },
  {
    title: "Herramientas y Utilidades",
    image: "/financeflow.svg",
    desc: "Funciones de apoyo para acelerar operaciones y mejorar el flujo diario.",
    points: [
      "Catálogo de productos y servicios",
      "Buscador global en toda la app",
      "Roles y permisos granulares",
      "Notificaciones internas y recordatorios",
    ],
  },
  {
    title: "Administración SaaS (God Mode)",
    image: "/financeflow.svg",
    desc: "Panel de super administrador para gestionar usuarios, suscripciones y soporte.",
    points: [
      "Dashboard global de métricas",
      "Gestión de usuarios del SaaS",
      "Impersonation para soporte",
      "Control de suscripciones y cupones",
      "Estados: Activo, Baneado, Demo",
    ],
  },
  {
    title: "Arquitectura y Base Técnica",
    image: "/financeflow.svg",
    desc: "Estructura pensada para crecer por módulos y mantener orden en backend y frontend.",
    points: [
      "Laravel 12 + PHP 8.2 + MySQL",
      "Blade + TailwindCSS + Alpine.js",
      "Eloquent models para dominio completo",
      "Middleware de permisos y suscripción",
      "PDF con dompdf + pagos con Stripe",
    ],
  },
];

const views = [
  "Dashboard principal (resumen financiero + hucha fiscal)",
  "Facturas (listado, filtros, estados y acciones)",
  "Crear/Editar factura (líneas dinámicas con Alpine.js)",
  "Presupuestos y conversión a factura",
  "Gastos + cuadro fiscal (IVA/IRPF)",
  "Clientes, empleados, roles y permisos",
  "Control horario con mapa y geolocalización",
  "Panel de admin SaaS (God Mode)",
];
---

<Layout>
  <main class="min-h-screen bg-zinc-50 text-zinc-900">
    <!-- Header -->
    <section class="border-b border-zinc-200 bg-white">
      <div class="mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-12 sm:py-14">
        <p class="text-xs font-semibold uppercase tracking-[0.18em] text-zinc-500">
          Proyecto · SaaS
        </p>

        <h1 class="mt-3 text-3xl sm:text-4xl font-bold tracking-tight text-zinc-950">
          FinanceFlow
        </h1>

        <p class="mt-4 max-w-3xl text-sm sm:text-base leading-relaxed text-zinc-600">
          SaaS para autónomos y PYMES que centraliza facturación, gastos, presupuestos,
          clientes, empleados, control horario y administración del propio producto
          desde una interfaz moderna y clara.
        </p>

        <div class="mt-6 flex flex-wrap gap-2">
          <span class="rounded-full border border-zinc-300 bg-white px-3 py-1 text-xs text-zinc-700">
            Laravel 12
          </span>
          <span class="rounded-full border border-zinc-300 bg-white px-3 py-1 text-xs text-zinc-700">
            Blade + TailwindCSS
          </span>
          <span class="rounded-full border border-zinc-300 bg-white px-3 py-1 text-xs text-zinc-700">
            Alpine.js
          </span>
          <span class="rounded-full border border-zinc-300 bg-white px-3 py-1 text-xs text-zinc-700">
            Stripe
          </span>
          <span class="rounded-full border border-zinc-300 bg-white px-3 py-1 text-xs text-zinc-700">
            dompdf
          </span>
        </div>

        <div class="mt-6 text-sm text-zinc-600">
          <span class="font-medium text-zinc-800">Estado:</span> En desarrollo · Repositorio privado · Sin despliegue público
        </div>
      </div>
    </section>

    <!-- Módulos -->
    <section class="mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-10 sm:py-12">
      <div class="mb-6">
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-zinc-950">
          Módulos principales
        </h2>
        <p class="mt-2 text-sm sm:text-base text-zinc-600">
          Cada tarjeta muestra un módulo del producto con una captura y sus funcionalidades clave.
        </p>
      </div>

      <div class="grid gap-6 lg:grid-cols-2">
        {modules.map((module) => (
          <article class="overflow-hidden rounded-2xl border border-zinc-200 bg-white">
            <button
              type="button"
              data-lightbox
              class="block w-full border-b border-zinc-200 bg-zinc-100"
              aria-label={`Ampliar imagen de ${module.title}`}
            >
              <div class="aspect-[16/9] w-full">
                <img
                  src={module.image}
                  alt={`Captura del módulo ${module.title}`}
                  class="h-full w-full object-cover"
                  loading="lazy"
                />
              </div>
            </button>

            <div class="p-5 sm:p-6">
              <h3 class="text-lg sm:text-xl font-semibold text-zinc-900">
                {module.title}
              </h3>

              <p class="mt-2 text-sm leading-relaxed text-zinc-600">
                {module.desc}
              </p>

              <ul class="mt-4 space-y-2">
                {module.points.map((point) => (
                  <li class="flex items-start gap-2 text-sm text-zinc-700">
                    <span class="mt-[6px] h-1.5 w-1.5 rounded-full bg-zinc-400 shrink-0"></span>
                    <span>{point}</span>
                  </li>
                ))}
              </ul>
            </div>
          </article>
        ))}
      </div>
    </section>

    <!-- Vistas incluidas -->
    <section class="border-t border-zinc-200 bg-white">
      <div class="mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-10 sm:py-12">
        <div class="grid gap-8 lg:grid-cols-[1.2fr_1fr]">
          <div>
            <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-zinc-950">
              Vistas y estructura frontend
            </h2>
            <p class="mt-3 text-sm sm:text-base leading-relaxed text-zinc-600">
              La interfaz está construida con Blade, TailwindCSS y Alpine.js, organizada por módulos
              para facilitar mantenimiento y escalado del producto.
            </p>

            <div class="mt-5 rounded-xl border border-zinc-200 bg-zinc-50 p-4 sm:p-5">
              <p class="text-sm font-medium text-zinc-800">Layouts principales</p>
              <ul class="mt-3 space-y-2 text-sm text-zinc-700">
                <li><span class="font-medium">app.blade.php</span> · Shell privada (sidebar + topbar)</li>
                <li><span class="font-medium">guest.blade.php</span> · Shell pública / auth</li>
                <li><span class="font-medium">navigation.blade.php</span> · Navegación responsive</li>
              </ul>
            </div>
          </div>

          <div class="rounded-xl border border-zinc-200 bg-zinc-50 p-4 sm:p-5">
            <p class="text-sm font-medium text-zinc-800">Ejemplos de vistas destacadas</p>
            <ul class="mt-3 space-y-2">
              {views.map((view) => (
                <li class="flex items-start gap-2 text-sm text-zinc-700">
                  <span class="mt-[6px] h-1.5 w-1.5 rounded-full bg-zinc-400 shrink-0"></span>
                  <span>{view}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Stack técnico -->
    <section class="mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-10 sm:py-12">
      <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-zinc-950">
        Stack técnico
      </h2>

      <div class="mt-6 grid gap-4 md:grid-cols-2">
        <div class="rounded-xl border border-zinc-200 bg-white p-5">
          <h3 class="text-sm font-semibold uppercase tracking-wider text-zinc-500">Backend</h3>
          <ul class="mt-3 space-y-2 text-sm text-zinc-700">
            <li>Laravel Framework 12.x</li>
            <li>PHP 8.2+</li>
            <li>MySQL 8.0+</li>
            <li>laravel/breeze (auth)</li>
            <li>barryvdh/laravel-dompdf (PDF)</li>
            <li>stripe/stripe-php (pagos)</li>
          </ul>
        </div>

        <div class="rounded-xl border border-zinc-200 bg-white p-5">
          <h3 class="text-sm font-semibold uppercase tracking-wider text-zinc-500">Frontend</h3>
          <ul class="mt-3 space-y-2 text-sm text-zinc-700">
            <li>Blade</li>
            <li>TailwindCSS 3</li>
            <li>Alpine.js 3</li>
            <li>Vite</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Lightbox simple -->
    <div
      id="lightbox"
      class="fixed inset-0 z-[9999] hidden items-center justify-center bg-black/80 px-4 py-8"
      aria-hidden="true"
    >
      <div class="relative w-full max-w-5xl">
        <button
          id="lightboxClose"
          type="button"
          class="absolute -top-10 right-0 rounded-full border border-white/20 bg-white/10 px-3 py-1.5 text-xs font-semibold text-white/90 transition hover:bg-white/20"
        >
          Cerrar
        </button>

        <img
          id="lightboxImage"
          src=""
          alt=""
          class="w-full rounded-2xl border border-white/20 bg-black object-contain max-h-[85vh]"
        />
      </div>
    </div>
  </main>
</Layout>

<script is:inline>
  const lightbox = document.getElementById("lightbox");
  const lightboxImage = document.getElementById("lightboxImage");
  const lightboxClose = document.getElementById("lightboxClose");

  const openLightbox = (src, alt) => {
    if (!lightbox || !lightboxImage) return;
    lightboxImage.src = src;
    lightboxImage.alt = alt || "Vista ampliada";
    lightbox.classList.remove("hidden");
    lightbox.classList.add("flex");
    lightbox.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  };

  const closeLightbox = () => {
    if (!lightbox || !lightboxImage) return;
    lightbox.classList.add("hidden");
    lightbox.classList.remove("flex");
    lightbox.setAttribute("aria-hidden", "true");
    lightboxImage.src = "";
    lightboxImage.alt = "";
    document.body.style.overflow = "";
  };

  document.querySelectorAll("[data-lightbox]").forEach((trigger) => {
    trigger.addEventListener("click", () => {
      const img = trigger.querySelector("img");
      const src = img?.getAttribute("src");
      const alt = img?.getAttribute("alt");
      if (src) openLightbox(src, alt);
    });
  });

  if (lightbox) {
    lightbox.addEventListener("click", (event) => {
      if (event.target === lightbox) closeLightbox();
    });
  }

  if (lightboxClose) {
    lightboxClose.addEventListener("click", closeLightbox);
  }

  window.addEventListener("keydown", (event) => {
    if (event.key === "Escape") closeLightbox();
  });
</script>