---
import Layout from "../layouts/Layout.astro";

const gallery = [
  {
    title: "Dashboard principal",
    desc: "Resumen financiero y estado del negocio.",
  },
  {
    title: "Facturacion",
    desc: "Ciclo completo de presupuestos y facturas.",
  },
  {
    title: "Gastos e impuestos",
    desc: "Control fiscal y previsiones.",
  },
  {
    title: "Clientes y CRM",
    desc: "Historial, deuda y comunicaciones.",
  },
];

const sections = [
  {
    title: "Panel principal",
    desc: "Vista general con KPIs, alertas y accesos rapidos a lo esencial.",
  },
  {
    title: "Facturacion y presupuestos",
    desc: "Flujo agil para crear, enviar y convertir presupuestos en facturas.",
  },
  {
    title: "Gastos e impuestos",
    desc: "Registro de gastos, categorias y calculos fiscales en tiempo real.",
  },
  {
    title: "Clientes y productos",
    desc: "Fichas completas, historiales y catalogo listo para facturar.",
  },
  {
    title: "Equipo y control horario",
    desc: "Gestion de empleados, roles y fichajes con ubicacion.",
  },
  {
    title: "Administracion SaaS",
    desc: "Panel de control para suscripciones, usuarios y soporte.",
  },
];
---

<Layout>
  <main id="top" class="min-h-screen bg-sky-700 text-white">
    <div class="bg-linear-to-b from-cyan-700 via-cyan-950 to-zinc-950">
      <section class="relative overflow-hidden">
        <div
          aria-hidden="true"
          class="pointer-events-none absolute inset-0 z-0 opacity-25
                 bg-[radial-gradient(rgba(255,255,255,0.70)_1px,transparent_1px)]
                 bg-size-[18px_18px]"
        >
        </div>

        <div class="relative z-10 mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-16 sm:py-20 md:py-24">
          <div class="flex flex-col gap-6">
            <div class="flex flex-wrap items-center gap-2 text-xs font-semibold uppercase tracking-wider text-white/70">
              <span class="h-px w-8 bg-white/30"></span>
              Proyecto destacado
              <span class="h-px w-8 bg-white/30"></span>
            </div>

            <h1 class="text-3xl xs:text-4xl md:text-5xl font-extrabold leading-tight">
              SaaS en desarrollo
            </h1>

            <p class="max-w-3xl text-base sm:text-lg text-white/80 leading-relaxed">
              SaaS para autonomos y PYMES. Centraliza facturacion, gastos, clientes, equipo y
              administracion desde una interfaz moderna y clara.
            </p>

            <div class="flex flex-wrap gap-3">
              <a
                href="/#proyectos"
                class="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-4 py-2 text-sm font-semibold text-white/90
                       backdrop-blur-sm transition hover:bg-white/15 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/45"
              >
                Volver a proyectos
              </a>
              <a
                href="/#contacto"
                class="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-4 py-2 text-sm font-semibold text-white/90
                       backdrop-blur-sm transition hover:bg-white/15 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/45"
              >
                Contactar
              </a>
            </div>

            <div class="rounded-2xl border border-white/12 bg-white/10 p-4 text-sm text-white/70">
              Estado del proyecto: en desarrollo. Repositorio privado y sin despliegue publico.
            </div>

            <div class="mt-2 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
              {gallery.map((item) => (
                <figure class="rounded-2xl border border-white/12 bg-white/10 p-3 shadow-md shadow-black/20">
                  <div
                    class="flex items-center justify-center rounded-xl border border-white/10 bg-white/5 text-white/60
                           h-36 text-xs"
                  >
                    Placeholder imagen
                  </div>
                  <figcaption class="mt-3 text-sm font-semibold text-white/85">
                    {item.title}
                  </figcaption>
                  <p class="mt-1 text-xs text-white/60">{item.desc}</p>
                </figure>
              ))}
            </div>
          </div>
        </div>
      </section>

      <section class="relative overflow-hidden">
        <div class="relative z-10 mx-auto max-w-6xl px-4 sm:px-6 md:px-10 py-12 sm:py-16">
          <div class="grid gap-8">
            <div>
              <h2 class="text-2xl sm:text-3xl font-extrabold">Apartados del producto</h2>
              <p class="mt-3 text-sm sm:text-base text-white/75 max-w-3xl">
                Cada modulo tiene su propia vista con foco en claridad y rapidez de uso.
              </p>
            </div>

            <div class="grid gap-8">
              {sections.map((item, index) => (
                <article class="rounded-3xl border border-white/12 bg-white/10 p-5 sm:p-6 shadow-lg shadow-black/25">
                  <div class="grid gap-6 md:grid-cols-12 md:items-center">
                    <div
                      class={`md:col-span-7 ${
                        index % 2 === 1 ? "md:order-2" : "md:order-1"
                      }`}
                    >
                      <div
                        class="flex items-center justify-center rounded-2xl border border-white/12 bg-white/5 text-white/60
                               h-52 sm:h-64 md:h-72 text-sm"
                      >
                        Placeholder imagen
                      </div>
                    </div>

                    <div
                      class={`md:col-span-5 ${
                        index % 2 === 1 ? "md:order-1" : "md:order-2"
                      }`}
                    >
                      <p class="text-xs font-semibold uppercase tracking-wider text-white/60">
                        Apartado
                      </p>
                      <h3 class="mt-2 text-xl sm:text-2xl font-extrabold">
                        {item.title}
                      </h3>
                      <p class="mt-3 text-sm sm:text-base text-white/75">
                        {item.desc}
                      </p>
                    </div>
                  </div>
                </article>
              ))}
            </div>
          </div>
        </div>
      </section>
    </div>
  </main>
</Layout>
